# Copy this file to ".env" in the project root (never commit .env).
# Flutter loads .env at startup; you can also use --dart-define=KEY=value for CI.

SUPABASE_URL=
SUPABASE_ANON_KEY=

GEMINI_API_KEY=
GOOGLE_MAPS_API_KEY=
PLACES_API_KEY=

# Android FCM: place android/app/google-services.json from Firebase Console (do not commit if you gitignore it).

import 'package:flutter/material.dart';

/// Brand: periwinkle blue #7289FF — used as primary across the app.
abstract final class AppColors {
  static const Color primary = Color(0xFF7289FF);
  static const Color primaryDark = Color(0xFF5B6FD9);
  static const Color primaryLight = Color(0xFF9EAEFF);
  /// Deep indigo complement for contrast (secondary accents, teal replacement).
  static const Color secondary = Color(0xFF3D4A8C);
  static const Color surfaceLight = Color(0xFFF5F7FF);
  static const Color surfaceDark = Color(0xFF0A0B12);
  static const Color cardDark = Color(0xFF141622);
  static const Color glowBrand = Color(0x407289FF);
}
